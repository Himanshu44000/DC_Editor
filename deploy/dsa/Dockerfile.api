FROM node:22-bullseye-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY database ./database

ENV NODE_ENV=production
EXPOSE 4000

CMD ["node", "server/index.js"]
